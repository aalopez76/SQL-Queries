# --- SQLite: temporary files (we do NOT ignore the .sqlite DB: it is the portfolio dataset) ---
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.db-wal
*.db-shm

# --- sqlfluff / Python (host of the lint tooling) ---
__pycache__/
*.pyc
.venv/
venv/
.sqlfluff_cache/

# --- Operating system ---
Thumbs.db
desktop.ini
.DS_Store

# --- Editors ---
.vscode/
.idea/
*.swp

# --- Local validation outputs (regenerable) ---
docs/data_report.local.md

# --- Internal working notes / AI tooling (kept local, not published) ---
CLAUDE.md
handoff.md
AUDIT.md
REFACTOR_PLAN.md
.claude/

# --- Presentation artifacts (live on the personal page, not in the code repo) ---
*.pdf
